---
import ArticlePage from '../layouts/ArticlePage.astro';
import { getCopy } from '../copy';

const lang = 'en';
const copy = getCopy(lang).about;
---

<ArticlePage copy={copy} lang={lang} pageKey="/about" />
