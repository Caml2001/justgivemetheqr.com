---
import ArticlePage from '../../layouts/ArticlePage.astro';
import { getCopy } from '../../copy';

const lang = 'es';
const copy = getCopy(lang).contact;
---

<ArticlePage copy={copy} lang={lang} pageKey="/contact" />
